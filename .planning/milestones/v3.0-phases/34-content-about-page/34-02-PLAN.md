---
phase: 34-content-about-page
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/pages/AboutPage.tsx
  - src/components/about/DedicationCard.tsx
  - src/AppShell.tsx
  - src/components/navigation/navConfig.ts
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-06

must_haves:
  truths:
    - "Navigating to #/about renders a full About page with narrative sections and dedication cards"
    - "The About page is a public page (no ProtectedRoute wrapper)"
    - "Dedication cards show brief tribute by default and expand to full tribute on tap"
    - "The page renders correctly in English-only mode (showBurmese=false)"
    - "The page renders correctly in bilingual mode (showBurmese=true) with font-myanmar on all Burmese text"
    - "The page has a full-screen mobile reading experience (no sidebar or bottom nav bar)"
    - "Footer shows version, year, open source notice, and GitHub repo link"
  artifacts:
    - path: "src/pages/AboutPage.tsx"
      provides: "About page with narrative, dedications, call to action, footer"
      min_lines: 80
    - path: "src/components/about/DedicationCard.tsx"
      provides: "Tap-to-expand bilingual dedication card component"
      min_lines: 40
  key_links:
    - from: "src/pages/AboutPage.tsx"
      to: "src/constants/about/aboutContent.ts"
      via: "import aboutContent"
      pattern: "import.*aboutContent.*from.*constants/about"
    - from: "src/pages/AboutPage.tsx"
      to: "src/components/about/DedicationCard.tsx"
      via: "DedicationCard component usage"
      pattern: "<DedicationCard"
    - from: "src/AppShell.tsx"
      to: "src/pages/AboutPage.tsx"
      via: "Route path='/about' registration"
      pattern: "path=.*about"
    - from: "src/components/navigation/navConfig.ts"
      to: "HIDDEN_ROUTES"
      via: "'/about' added to array"
      pattern: "/about"
---

<objective>
Build the About page UI with DedicationCard component, register the route, and hide nav on the About page.

Purpose: Render the narrative, dedications, call to action, and footer as a beautiful, bilingual, full-screen reading experience accessible at `#/about`.
Output: `AboutPage.tsx`, `DedicationCard.tsx`, route in `AppShell.tsx`, `/about` in `HIDDEN_ROUTES`.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-content-about-page/34-CONTEXT.md
@.planning/phases/34-content-about-page/34-RESEARCH.md
@.planning/phases/34-content-about-page/34-01-SUMMARY.md
@src/pages/OpEdPage.tsx — reference pattern for public page with GlassHeader
@src/components/navigation/GlassHeader.tsx — glass-morphism header component
@src/components/navigation/navConfig.ts — HIDDEN_ROUTES array
@src/AppShell.tsx — route registration
@src/hooks/useReducedMotion.ts — reduced motion hook
@src/components/animations/StaggeredList.tsx — FadeIn animation component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DedicationCard component</name>
  <files>src/components/about/DedicationCard.tsx</files>
  <action>
Create `src/components/about/DedicationCard.tsx` — a tap-to-expand bilingual dedication card.

**Pattern:** Follow the WhyButton/ExplanationCard expand pattern from the codebase (AnimatePresence + height: 'auto' animation).

**Props interface:**
```typescript
interface DedicationPerson {
  name: { en: string; my: string };
  role: { en: string; my: string };
  briefTribute: { en: string; my: string };
  fullTribute: { en: string; my: string };
}

interface DedicationCardProps {
  person: DedicationPerson;
}
```

**Implementation details:**
- Import `useState` from react, `motion, AnimatePresence` from `motion/react`, `ChevronDown` from `lucide-react`
- Import `useReducedMotion` from `@/hooks/useReducedMotion`
- Import `useLanguage` from `@/contexts/LanguageContext`
- Use `useState(false)` for `isExpanded`
- Collapsed state shows: name (en + conditional my with font-myanmar), role (en), brief tribute (en + conditional my with font-myanmar)
- Expand chevron rotates 180 degrees when expanded
- Expanded state adds: full tribute (en + conditional my with font-myanmar), inside a border-t separator
- All Burmese text elements MUST have `className="font-myanmar"`
- Respect reduced motion: skip height animation, use opacity-only for `shouldReduceMotion`
- Card styling: `rounded-2xl border border-border/60 bg-card overflow-hidden shadow-lg`
- Button: `min-h-[44px]` touch target, `aria-expanded` attribute, hover and focus-visible states
- Use CSS custom properties for colors (no hex — stylelint enforces `color-no-hex: true`)
- Add a subtle Heart icon or decorative element to give the card visual emphasis and warmth (Claude's Discretion on exact treatment)

**Entrance animation (Claude's Discretion):**
- Use FadeIn wrapper or simple motion.div fade-in on mount
- Keep it respectful and consistent with the app's motion system — not flashy
  </action>
  <verify>
Run: `npx tsc --noEmit` — no TypeScript errors.
Verify the component has aria-expanded, font-myanmar on Burmese text, and reduced motion support.
  </verify>
  <done>DedicationCard renders collapsed brief tribute by default, expands to full tribute on tap, renders bilingually with font-myanmar, respects reduced motion, meets 44px touch target.</done>
</task>

<task type="auto">
  <name>Task 2: Create AboutPage and register route</name>
  <files>
    src/pages/AboutPage.tsx
    src/AppShell.tsx
    src/components/navigation/navConfig.ts
  </files>
  <action>
**AboutPage.tsx:**

Create `src/pages/AboutPage.tsx` — the full About page.

Structure (Claude's Discretion on section ordering):
1. `GlassHeader` with `showBack backHref="/"` — provides back button for mobile reading
2. Hero section — title + subtitle from aboutContent.hero
3. Narrative sections — map over `aboutContent.sections`, rendering title + paragraphs for each
4. Dedications section — section title + two DedicationCard components (Clark, Guyots)
5. Call to action — share the app section with share button (use navigator.share if available, fallback to copy link)
6. External links — 2-3 links to stable resources (USCIS, VIA)
7. Footer — version, year, open source notice, GitHub link

**Key implementation details:**
- Import `GlassHeader` from `@/components/navigation/GlassHeader`
- Import `useLanguage` from `@/contexts/LanguageContext`
- Import `aboutContent` from `@/constants/about/aboutContent`
- Import `DedicationCard` from `@/components/about/DedicationCard`
- Import `FadeIn` from `@/components/animations/StaggeredList`
- Import `Heart, ExternalLink, Share2` from `lucide-react`
- Use `const { showBurmese } = useLanguage()` for bilingual rendering
- Pattern: English text always shown, Burmese text conditionally rendered with `{showBurmese && ...}` blocks
- ALL Burmese text elements must have `font-myanmar` class
- Use `page-shell` class on outermost div (matches OpEdPage pattern)
- Main content: `max-w-3xl mx-auto px-4 py-8 sm:px-6 space-y-10`
- Use FadeIn wrappers for entrance animations on each section
- Share button: try `navigator.share({ title, url })` first, fall back to `navigator.clipboard.writeText(url)` with a toast-like feedback
- External links: open in new tab with `target="_blank" rel="noreferrer"`
- Footer: centered, muted text, smaller size, border-t separator
- No hex colors — use design tokens only

**AppShell.tsx:**
- Add `import AboutPage from '@/pages/AboutPage';` with other page imports
- Add route BEFORE the catch-all: `<Route path="/about" element={<AboutPage />} />`
- The route must NOT be wrapped in ProtectedRoute — About is a public page

**navConfig.ts:**
- Add `'/about'` to the HIDDEN_ROUTES array to hide sidebar and bottom nav on the About page
- This ensures the full-screen mobile reading experience per CONTEXT.md

  </action>
  <verify>
Run: `npx tsc --noEmit` — no TypeScript errors.
Run: `npm run lint` — no lint errors.
Verify route is registered before catch-all in AppShell.tsx.
Verify '/about' is in HIDDEN_ROUTES array.
Verify AboutPage does NOT use ProtectedRoute wrapper.
  </verify>
  <done>About page renders at #/about with full narrative, two dedication cards, share CTA, external links, and footer. Nav is hidden for full-screen reading. Page is publicly accessible without authentication.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `npm run build` succeeds
- Route `/about` is registered in AppShell.tsx before catch-all, not wrapped in ProtectedRoute
- `/about` is in HIDDEN_ROUTES in navConfig.ts
- DedicationCard has tap-to-expand with AnimatePresence
- All Burmese text has font-myanmar class
- No hex colors used anywhere
</verification>

<success_criteria>
1. AboutPage renders the complete narrative, dedications, CTA, and footer at #/about
2. DedicationCards expand/collapse on tap with reduced motion support
3. Page works in both English-only and bilingual modes
4. No sidebar or bottom nav visible on the About page (HIDDEN_ROUTES)
5. Page is accessible without authentication
</success_criteria>

<output>
After completion, create `.planning/phases/34-content-about-page/34-02-SUMMARY.md`
</output>
