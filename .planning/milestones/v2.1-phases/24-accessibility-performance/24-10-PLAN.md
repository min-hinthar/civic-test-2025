---
phase: 24-accessibility-performance
plan: 10
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05", "24-06", "24-07", "24-08", "24-09"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All phase changes verified working together in the running app"
    - "Screen reader announcements fire correctly in quiz flow"
    - "Per-question timer works in both mock test and practice mode"
    - "Flashcard flip and sort swipe work on mobile without bugs"
    - "Build passes cleanly"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint: build the app, run all tests, and verify the integrated Phase 24 changes work together.

Purpose: Catch integration issues before shipping.
Output: Verified build, all tests passing, visual confirmation of key changes.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-accessibility-performance/24-01-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-02-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-03-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-04-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-05-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-06-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-07-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-08-SUMMARY.md
@.planning/phases/24-accessibility-performance/24-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and test suite verification</name>
  <files></files>
  <action>
    1. Run full test suite: `pnpm test:run`
    2. Run typecheck: `pnpm run typecheck`
    3. Run lint: `pnpm run lint`
    4. Run build: `pnpm run build`
    5. Run a11y tests specifically: `pnpm test:run -- src/__tests__/a11y/`
    6. Fix any failures found. Common issues:
       - Import path mismatches
       - Missing type exports
       - ESLint jsx-a11y violations introduced by other plans
       - Build-time errors from next.config.mjs changes
  </action>
  <verify>
    - All 4 commands pass cleanly
    - Zero test failures
    - Zero typecheck errors
    - Zero lint errors (warnings acceptable)
    - Build produces valid output
  </verify>
  <done>Full build, typecheck, lint, and test suite pass with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 24 implements:
    1. **A11Y Tooling**: eslint-plugin-jsx-a11y linting + vitest-axe accessibility tests
    2. **Screen Reader**: FeedbackPanel verdict announcements, progress bar segment labels, toast role splitting, language toggle full context, celebration announcements
    3. **Focus Management**: Skip-to-content link, focus after Check/Continue
    4. **High Contrast**: prefers-contrast: more token overrides
    5. **Reduced Motion**: Crossfade flashcard flip, quick-slide sort swipe, static celebrations, fade countdown
    6. **Per-Question Timer**: 30s circular timer with green/yellow/red urgency, 5s warning tick, auto-submit on expiry, pause during feedback
    7. **Timer Extension (WCAG 2.2.1)**: Extension toast at 6s in practice mode, +15s button, E keyboard shortcut, unlimited extensions
    8. **Performance**: Sentry Web Vitals (LCP/INP/CLS), bundle analyzer, Myanmar font optimization, SW caching audit
    9. **Flashcard Fixes**: backfaceVisibility, flip flickering, deck transparency, layout shift, swipe threshold, animation freeze, overlay interference, auto-read timing, speech button background
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Open http://localhost:3000

    **Screen Reader (if VoiceOver/NVDA available):**
    - Start a practice quiz, check an answer -- hear verdict announcement
    - Tab through progress bar segments -- hear individual labels
    - Toggle language -- hear full context announcement

    **Per-Question Timer:**
    - Start a mock test -- see BOTH overall timer and per-question 30s timer
    - Start a practice quiz with timer ON -- see 30s timer
    - Wait for timer to reach 5s -- hear tick sound, see red color
    - In practice, see extension toast at 6s, tap "+15s" -- timer extends
    - Let timer expire with no answer -- question auto-skips
    - Let timer expire with answer selected -- answer auto-submits

    **Flashcard:**
    - Go to Study Guide, open a category, view flashcards
    - Flip a card -- no flickering, back face hidden properly
    - Cards below are opaque, no layout shift during flip
    - Sort mode: swipe cards -- responsive on mobile viewport

    **Reduced Motion:**
    - Enable reduced motion in OS settings
    - Flip flashcard -- crossfade instead of 3D rotation
    - Sort cards -- quick slide instead of spring physics

    **Toast:**
    - Trigger an error action -- assertive toast
    - Complete a successful action -- polite toast

    3. Type "approved" or describe any issues
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- All tests pass
- A11y tests pass
- Human verifies key flows in browser
</verification>

<success_criteria>
- Zero build/test/lint errors
- Human confirms screen reader, timer, flashcard, and reduced motion changes work
</success_criteria>

<output>
After completion, create `.planning/phases/24-accessibility-performance/24-10-SUMMARY.md`
</output>
