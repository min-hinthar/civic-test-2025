---
phase: 10-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/pages/TestPage.tsx
  - src/pages/StudyGuidePage.tsx
  - src/constants/civicsQuestions.ts
  - src/components/ui/use-toast.ts
  - src/components/ui/Toast.tsx
  - src/components/ui/toaster.tsx
autonomous: true

must_haves:
  truths:
    - "No file imports from @/constants/civicsQuestions path"
    - "No unused Radix toast system files remain in the codebase"
    - "TypeScript compiles with zero errors"
    - "All existing tests pass"
  artifacts:
    - path: "src/pages/TestPage.tsx"
      provides: "Uses allQuestions from @/constants/questions (no deprecated import)"
      contains: "allQuestions.*@/constants/questions"
    - path: "src/pages/StudyGuidePage.tsx"
      provides: "Uses allQuestions from @/constants/questions (no deprecated import)"
      contains: "allQuestions.*@/constants/questions"
  key_links:
    - from: "src/pages/TestPage.tsx"
      to: "src/constants/questions/index.ts"
      via: "import { allQuestions } from '@/constants/questions'"
      pattern: "allQuestions.*@/constants/questions"
    - from: "src/pages/StudyGuidePage.tsx"
      to: "src/constants/questions/index.ts"
      via: "import { allQuestions } from '@/constants/questions'"
      pattern: "allQuestions.*@/constants/questions"
---

<objective>
Clean up deprecated imports and remove dead code: convert 2 files from `@/constants/civicsQuestions` to `@/constants/questions`, delete the compatibility layer, and remove the now-unused Radix toast system files.

Purpose: After plan 10-01 converts all toast callsites to BilingualToast, the Radix toast files (use-toast.ts, Toast.tsx, toaster.tsx) become dead code (~230 lines). The civicsQuestions compatibility layer is also dead weight that misleads future development. Removing both reduces confusion and maintenance burden.

Output: 2 modified source files, 4 deleted files.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-RESEARCH.md
@.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md

Key reference:
@src/constants/civicsQuestions.ts (the compatibility layer to delete)
@src/constants/questions/index.ts (the canonical import path)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deprecated civicsQuestions imports to allQuestions</name>
  <files>
    src/pages/TestPage.tsx
    src/pages/StudyGuidePage.tsx
    src/constants/civicsQuestions.ts
  </files>
  <action>
    **TestPage.tsx:**
    1. Remove: `import { civicsQuestions } from '@/constants/civicsQuestions'`
    2. The file already has `import { allQuestions } from '@/constants/questions'` (line ~35 per research). Confirm this import exists.
    3. Find all references to `civicsQuestions` in the file and replace with `allQuestions`.
    4. If there are no remaining references to `civicsQuestions` after replacement, the deprecated import removal is complete.

    **StudyGuidePage.tsx:**
    1. Remove: `import { civicsQuestions } from '@/constants/civicsQuestions'`
    2. Add: `import { allQuestions } from '@/constants/questions'`
    3. Replace ALL references to `civicsQuestions` with `allQuestions` throughout the file (~9 references per research at lines 51, 59, 91, 92, 157, 284, 462, 731 and others).
    4. Use find-and-replace for the identifier `civicsQuestions` -> `allQuestions` in this file.

    **Delete civicsQuestions.ts:**
    After both files are converted, delete `src/constants/civicsQuestions.ts`.

    **Final verification grep:**
    Run `grep -r "civicsQuestions" src/` to confirm zero remaining references anywhere.
    If any other files reference `civicsQuestions`, convert them too.
  </action>
  <verify>
    `grep -r "civicsQuestions" src/` returns ZERO results.
    `npx tsc --noEmit` -- zero errors.
    `npm run lint` -- no new errors.
    `npx vitest run` -- all tests pass.
    Confirm `src/constants/civicsQuestions.ts` does NOT exist: `ls src/constants/civicsQuestions.ts` should fail.
  </verify>
  <done>
    Both TestPage and StudyGuidePage use `allQuestions` from `@/constants/questions`. The civicsQuestions.ts compatibility layer is deleted. Zero references to the deprecated path remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused Radix toast system files</name>
  <files>
    src/components/ui/use-toast.ts
    src/components/ui/Toast.tsx
    src/components/ui/toaster.tsx
  </files>
  <action>
    After plan 10-01 converted all callsites to BilingualToast, these 3 files are dead code:
    - `src/components/ui/use-toast.ts` -- the legacy toast() shim (console.warn)
    - `src/components/ui/Toast.tsx` -- Radix toast primitives (unused)
    - `src/components/ui/toaster.tsx` -- ToastContextProvider + Toaster (never mounted)

    **Before deleting, verify no remaining imports:**
    1. `grep -r "from '@/components/ui/use-toast'" src/` -- should be ZERO (10-01 removed all)
    2. `grep -r "from '@/components/ui/Toast'" src/` -- should be ZERO or only from toaster.tsx
    3. `grep -r "from '@/components/ui/toaster'" src/` -- should be ZERO (never mounted)

    If any imports remain, DO NOT delete -- report the finding and convert the import first.

    **Delete all 3 files:**
    - `rm src/components/ui/use-toast.ts`
    - `rm src/components/ui/Toast.tsx`
    - `rm src/components/ui/toaster.tsx`

    **Post-deletion verification:**
    `npx tsc --noEmit` must still pass with zero errors.
    `npm run lint` must pass.
  </action>
  <verify>
    `ls src/components/ui/use-toast.ts src/components/ui/Toast.tsx src/components/ui/toaster.tsx` -- all 3 files should NOT exist.
    `npx tsc --noEmit` -- zero errors.
    `npm run lint` -- no errors.
    `npx vitest run` -- all tests pass.
  </verify>
  <done>
    All 3 Radix toast system files deleted. No dangling imports remain. ~230 lines of dead code removed.
  </done>
</task>

</tasks>

<verification>
Run these checks to verify plan completion:

1. `grep -r "civicsQuestions" src/` -- ZERO results
2. `grep -r "from '@/components/ui/use-toast'" src/` -- ZERO results
3. `grep -r "from '@/components/ui/Toast'" src/` -- ZERO results
4. `grep -r "from '@/components/ui/toaster'" src/` -- ZERO results
5. `ls src/constants/civicsQuestions.ts` -- file does NOT exist
6. `ls src/components/ui/use-toast.ts` -- file does NOT exist
7. `npx tsc --noEmit` -- zero errors
8. `npm run lint` -- no errors
9. `npx vitest run` -- all tests pass
</verification>

<success_criteria>
- Zero files import from @/constants/civicsQuestions
- civicsQuestions.ts compatibility layer deleted
- 3 Radix toast system files deleted (use-toast.ts, Toast.tsx, toaster.tsx)
- TypeScript compiles with zero errors
- ESLint passes
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md`
</output>
