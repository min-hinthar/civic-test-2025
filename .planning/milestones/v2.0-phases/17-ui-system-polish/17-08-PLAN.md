---
phase: 17-ui-system-polish
plan: 08
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/pages/StudyGuidePage.tsx
  - src/pages/TestPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/PracticePage.tsx
  - src/pages/InterviewPage.tsx
  - src/components/practice/PracticeConfig.tsx
  - src/components/practice/PracticeSession.tsx
  - src/components/interview/InterviewSetup.tsx
  - src/components/srs/SessionSetup.tsx
autonomous: true

must_haves:
  truths:
    - "All interactive elements across all pages meet 44px minimum touch target size"
    - "Icon-only buttons are at least 48px in both width and height"
    - "Study guide question rows are full-width tappable cards with generous padding"
    - "Text links are converted to pill-shaped buttons with proper padding"
    - "Form elements (state picker, search, toggles) are at least 48px in height"
  artifacts:
    - path: "src/pages/StudyGuidePage.tsx"
      provides: "Study guide with tappable card-style rows"
      contains: "min-h-[44px]"
    - path: "src/pages/TestPage.tsx"
      provides: "Test page with 44px+ answer buttons"
      contains: "min-h-[44px]"
    - path: "src/pages/SettingsPage.tsx"
      provides: "Settings with 48px form elements"
      contains: "min-h-[48px]"
  key_links:
    - from: "src/pages/StudyGuidePage.tsx"
      to: "src/components/ui/Button.tsx"
      via: "Button component usage"
      pattern: "Button"
---

<objective>
Audit and fix all interactive elements across the app for 44px+ minimum touch targets. Convert undersized elements, text links to pill buttons, and study guide rows to tappable cards.

Purpose: Satisfies UISYS-03 (all interactive elements meet 44px minimum touch target). This is a cross-cutting accessibility requirement.
Output: Every tappable element in the app meets or exceeds 44px minimum touch target size.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-ui-system-polish/17-RESEARCH.md
@src/pages/StudyGuidePage.tsx
@src/pages/TestPage.tsx
@src/pages/SettingsPage.tsx
@src/pages/PracticePage.tsx
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Study guide rows to tappable cards and text link conversion</name>
  <files>src/pages/StudyGuidePage.tsx, src/components/practice/PracticeConfig.tsx, src/components/practice/PracticeSession.tsx</files>
  <action>
  **StudyGuidePage.tsx:**

  Read the current StudyGuidePage. Per user decision: "Study guide question rows: Convert to full-width tappable card-style rows with generous padding"

  1. Find the question list/row components. They might be `interactive-tile` class or list items.
  2. Convert each question row to a full-width tappable card:
     - Add `min-h-[56px]` for generous touch target (above 44px)
     - Add `p-4` padding for comfortable spacing
     - Add `rounded-xl` for card appearance
     - Apply `glass-light` class for the glass effect
     - Ensure the entire row is a single tap target (`<button>` or `<Link>` wrapping the full row)
     - Add `cursor-pointer` and hover state: `hover:bg-primary/5`
  3. For any inline text links (e.g., "View all", "See more"):
     - Convert to pill-shaped links: `inline-flex items-center rounded-full px-4 py-2 min-h-[36px] bg-primary/10 text-primary font-medium hover:bg-primary/20`
     - This is above 44px when accounting for line-height + padding
  4. Category header rows: ensure they have adequate padding (p-3 minimum)
  5. Search/filter input: ensure `min-h-[48px]` per user decision for form elements

  **PracticeConfig.tsx:**

  1. Read current implementation. Check all buttons and options:
     - Category selection buttons: ensure 44px+ height
     - Count selector: ensure buttons are 44px+
     - Start button: uses Button component which already has 44px min-height
  2. Any toggle switches: ensure 48px minimum height for the touch area

  **PracticeSession.tsx:**

  1. Audit answer option buttons: they should use the Button component (already 44px)
  2. Navigation buttons (Next, Previous): verify 44px+
  3. Any text links: convert to pill buttons
  </action>
  <verify>Run `npx next build` -- zero errors. Manually audit: grep for `min-h-\[` to verify touch target classes exist in modified files. Verify no inline text links remain without padding.</verify>
  <done>Study guide rows are full-width tappable cards with 56px height. Text links converted to pill-shaped buttons. Practice config/session elements meet 44px+.</done>
</task>

<task type="auto">
  <name>Task 2: Test page, settings, and interview touch target audit</name>
  <files>src/pages/TestPage.tsx, src/pages/SettingsPage.tsx, src/pages/InterviewPage.tsx, src/components/interview/InterviewSetup.tsx, src/components/srs/SessionSetup.tsx</files>
  <action>
  **TestPage.tsx:**

  Per user decision: "Mock test answer buttons: Claude audits and adjusts if below 44px minimum"

  1. Read the TestPage and find the answer option buttons
  2. If they use the Button component, they already have `min-h-[44px]` (md size)
  3. If they use custom button elements, add `min-h-[44px]` class
  4. The "Next Question" and "Submit Test" buttons: verify they use Button component with at least md size
  5. Pre-test screen buttons: verify 44px+

  **SettingsPage.tsx:**

  Per user decision: "Form elements (state picker, search, toggles): 48px minimum height"

  1. Read the SettingsPage
  2. State picker dropdown/select: add `min-h-[48px]` class
  3. Toggle switches: ensure the toggle container is at least 48px tall
  4. Any text inputs: add `min-h-[48px]`
  5. Sign out button: verify 44px+ (it likely uses Button component)
  6. Theme toggle in settings: verify 48px (icon button = 48px per user decision)

  **InterviewPage.tsx / InterviewSetup.tsx:**

  1. Read current implementations
  2. Mode selection buttons (practice/realistic): verify 44px+
  3. Start interview button: verify uses Button component
  4. Any toggle or radio options: ensure 44px+

  **SessionSetup.tsx (SRS):**

  1. Read the SRS session setup
  2. Review count selector: verify touch targets
  3. Start review button: verify 44px+
  4. Any selector controls: 48px minimum

  **General icon button audit across modified files:**

  For any icon-only buttons found (buttons with just an icon, no text):
  - Minimum 48px in both width and height per user decision
  - Apply `w-12 h-12` (48px) if currently `w-10 h-10` (40px) or smaller
  - This includes: speech buttons, add-to-deck buttons, share buttons, close buttons on modals
  </action>
  <verify>Run `npx next build` -- zero errors. Grep across all modified files for elements that might be below 44px (check for min-h values or h- classes below 44px that are interactive). Verify all form elements have min-h-[48px].</verify>
  <done>All test page answer buttons, settings form elements, interview setup options, and SRS controls meet 44px/48px touch target minimums. All icon-only buttons are 48px.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with zero errors
2. StudyGuidePage question rows have min-h of 56px
3. SettingsPage form elements have min-h-[48px]
4. TestPage answer buttons have min-h-[44px]
5. No icon-only button below 48px across modified files
6. No inline text links without pill-shaped padding
</verification>

<success_criteria>
Every interactive element in the app meets the 44px minimum touch target (48px for icon-only buttons and form elements). Study guide rows are card-style tappable surfaces. Text links are pill buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-system-polish/17-08-SUMMARY.md`
</output>
