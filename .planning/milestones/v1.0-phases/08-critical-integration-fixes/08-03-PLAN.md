---
phase: 08-critical-integration-fixes
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/pages/Dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to Settings from the Dashboard page"
    - "Settings icon is visually subtle (gear icon, not a full button) in the Dashboard header"
    - "Clicking the Settings icon navigates to /settings"
  artifacts:
    - path: "src/pages/Dashboard.tsx"
      provides: "Settings navigation link in header area"
      contains: "Settings"
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "/settings"
      via: "gear icon link in header"
      pattern: "navigate.*settings|to=.*settings"
---

<objective>
Add a Settings navigation link on the Dashboard page so users can discover and access the Settings page from the app.

Purpose: The Settings page exists with language, notifications, social, review reminders, and speech rate sections — but no page links to it. Users cannot discover or reach it. Adding a Dashboard link closes this gap.
Output: Gear icon in Dashboard header that navigates to /settings
</objective>

<execution_context>
@C:\Users\minkk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\minkk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-critical-integration-fixes/08-RESEARCH.md
@src/pages/Dashboard.tsx
@src/pages/SettingsPage.tsx
@src/lib/i18n/strings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Settings gear icon to Dashboard header</name>
  <files>
    src/pages/Dashboard.tsx
  </files>
  <action>
    Add a subtle Settings gear icon in the Dashboard header area, positioned at the top-right corner of the welcome section.

    **Implementation:**

    1. Import `Settings` icon from `lucide-react` (add to existing lucide imports in Dashboard.tsx)
    2. Import `strings` from `@/lib/i18n/strings` if not already imported (it likely is for existing bilingual content)
    3. In the `<header>` element (currently around line 287-302), wrap the existing content in a `flex justify-between items-start` container and add a Settings icon button on the right side:

    ```tsx
    <header className="mb-8">
      <FadeIn>
        <div className="flex items-start justify-between">
          <div>
            <BilingualHeading ... />
            <p className="mt-2 text-muted-foreground">...</p>
          </div>
          <Link
            to="/settings"
            className="flex h-10 w-10 items-center justify-center rounded-full border border-border/60 text-muted-foreground transition hover:bg-muted/40 hover:text-foreground"
            aria-label="Settings · ဆက်တင်များ"
          >
            <Settings className="h-5 w-5" />
          </Link>
        </div>
      </FadeIn>
    </header>
    ```

    **Design decisions (Claude's discretion):**
    - Use `Link` from react-router-dom (not `BilingualButton`) — Settings is a utility nav, not a primary action
    - Gear icon styled as a subtle circular button matching the theme toggle style in AppNavigation
    - `border-border/60` for subtle border, `text-muted-foreground` for subdued color
    - `hover:bg-muted/40 hover:text-foreground` for interactive feedback
    - Bilingual aria-label for accessibility: "Settings · ဆက်တင်များ"
    - Icon size `h-5 w-5` matches lucide icon sizing used elsewhere in the app
    - No bilingual visible text — the gear icon is universally understood; keeps the header clean
    - No other pages get cross-links to Settings — the SettingsPage already has `navigate(-1)` back button, and the Dashboard is the natural discovery point

    **No changes needed to SettingsPage.tsx** — it already has a working back button with `navigate(-1)` using an ArrowLeft icon (lines 64-68).
  </action>
  <verify>
    - `pnpm run typecheck` passes
    - `pnpm exec eslint src/pages/Dashboard.tsx --max-warnings=0` passes
    - `grep "settings" src/pages/Dashboard.tsx` shows the /settings Link
    - `grep "Settings" src/pages/Dashboard.tsx` shows the lucide Settings import and usage
    - The Settings link renders in the Dashboard header area as a gear icon
  </verify>
  <done>
    Dashboard header shows a subtle gear icon that navigates to /settings. The icon is accessible with a bilingual aria-label. Settings page is now discoverable from the main app navigation flow.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` — zero errors
2. `pnpm run lint` — zero errors
3. `pnpm run build` — succeeds
4. Dashboard page renders with a gear icon in the header area
5. Clicking the gear icon navigates to /settings
6. Settings page back button returns to Dashboard
</verification>

<success_criteria>
- Dashboard has a Settings gear icon in the header area
- Clicking it navigates to /settings
- The icon follows the app's design system (border, hover states, muted color)
- Accessible with bilingual aria-label
- No new lint or type errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/08-critical-integration-fixes/08-03-SUMMARY.md`
</output>
