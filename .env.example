# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Sentry
SENTRY_AUTH_TOKEN=
# Sentry DSN (public key, not a secret — allows per-environment DSNs and rotation)
NEXT_PUBLIC_SENTRY_DSN=

# Push Notifications (generate with: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:your-email@example.com

# Cron job authentication secret (for push notification sending)
CRON_SECRET=
# API key for SRS cron job endpoints (srs-reminder, weak-area-nudge)
SRS_CRON_API_KEY=
