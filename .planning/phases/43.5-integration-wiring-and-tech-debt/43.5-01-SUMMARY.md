---
phase: 43.5-integration-wiring-and-tech-debt
plan: 01
subsystem: ui
tags: [practice, search-params, google-auth, integration]
requirements-completed: [RDNS-04, RDNS-05, MIGR-12]

# Dependency graph
depends_on: []
unlocks: []
---

## What was built

Wired PracticePage `?category=` search param pre-selection and fixed GoogleOneTapSignIn stale redirect.

### Key changes

1. **Practice page Suspense boundary** — `app/(protected)/practice/page.tsx` wraps PracticePage in `<Suspense>` (required for `useSearchParams`)
2. **PracticePage search param reading** — reads `?category=` param, validates against USCIS_CATEGORY_NAMES and SUB_CATEGORY_NAMES, passes `initialCategory` to PracticeConfig
3. **PracticeConfig initial state** — accepts `initialCategory` prop, initializes `selectedCategory` and auto-expands parent accordion for sub-categories
4. **GoogleOneTapSignIn redirect** — changed fallback OAuth redirect from `/dashboard` to `/home` (canonical route since App Router migration)

### Key files

- `app/(protected)/practice/page.tsx` — Suspense boundary added
- `src/views/PracticePage.tsx` — useSearchParams + initialCategory derivation
- `src/components/practice/PracticeConfig.tsx` — initialCategory prop + state initialization
- `src/components/GoogleOneTapSignIn.tsx` — redirect fix /dashboard → /home

## Deviations

None.

## Self-Check: PASSED

- [x] All tasks executed
- [x] Build passes cleanly
- [x] Committed atomically
