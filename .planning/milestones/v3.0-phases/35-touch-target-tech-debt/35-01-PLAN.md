---
phase: 35-touch-target-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/navigation/GlassHeader.tsx
  - src/pages/AboutPage.tsx
  - src/lib/audio/celebrationSounds.ts
autonomous: true
requirements:
  - VISC-04

must_haves:
  truths:
    - "GlassHeader heart icon link has a minimum 44x44px touch area"
    - "AboutPage Share button meets 44x44px minimum touch target"
    - "AboutPage external links (Resources section) meet 44x44px minimum touch target"
    - "AboutPage GitHub footer link meets 44x44px minimum touch target"
    - "playXPDing and playErrorSoft functions no longer exist in the codebase"
  artifacts:
    - path: "src/components/navigation/GlassHeader.tsx"
      provides: "Heart icon link with min-h-[44px] min-w-[44px]"
      contains: "min-h-\\[44px\\]"
    - path: "src/pages/AboutPage.tsx"
      provides: "All interactive elements meet 44px minimum"
      contains: "min-h-\\[44px\\]"
    - path: "src/lib/audio/celebrationSounds.ts"
      provides: "Celebration sounds without orphaned exports"
  key_links:
    - from: "src/components/navigation/GlassHeader.tsx"
      to: "/about"
      via: "Link component with touch-safe dimensions"
      pattern: "min-h-\\[44px\\].*min-w-\\[44px\\]"
---

<objective>
Fix the 4 touch target violations introduced in Phase 34 and remove 2 orphaned celebration sound exports.

Purpose: Close the cross-phase VISC-04 gap — Phase 34 added new interactive elements (GlassHeader heart icon, AboutPage buttons/links) without enforcing the 44px minimum touch target rule established in Phase 29. Additionally, `playXPDing` and `playErrorSoft` were exported during Phase 32 but never imported by any consumer.

Output: 3 modified files with all Phase 35 success criteria items 1 and 2 resolved.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-touch-target-tech-debt/35-RESEARCH.md
@.planning/phases/29-visual-foundation/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 touch target violations in GlassHeader and AboutPage</name>
  <files>src/components/navigation/GlassHeader.tsx, src/pages/AboutPage.tsx</files>
  <action>
    Apply `min-h-[44px] min-w-[44px]` to 4 interactive elements that fall below the 44px WCAG touch target minimum. Follow the same pattern used in Phase 29-03 (ShareButton, AddToDeckButton, Dialog close button): add min-h/min-w alongside existing size classes so the visual appearance stays the same but the touch area expands.

    **GlassHeader heart icon (line ~48):**
    - Current: `h-9 w-9` (36x36px)
    - Add `min-h-[44px] min-w-[44px]` to the Link's className
    - Keep `h-9 w-9` for visual sizing — the min-h/min-w only expands the touch area

    **AboutPage Share button (line ~137):**
    - Current: `py-2.5` + text-sm (~40px tall)
    - Add `min-h-[44px]` to the button's className
    - No min-w needed — the button is wide enough horizontally

    **AboutPage external links in Resources section (line ~165):**
    - Current: `py-3` + text-sm (~38-40px)
    - Add `min-h-[44px]` to each external link anchor's className

    **AboutPage GitHub footer link (line ~205-213):**
    - Current: Inline text link with tiny icon (~20px tall)
    - Add `min-h-[44px]` and ensure `inline-flex items-center` for vertical centering
    - Add `px-2` padding if not already present for horizontal touch area

    IMPORTANT: Do NOT change `h-9` to `h-11` — that changes visual size. Use `min-h-[44px]` alongside existing height classes.
  </action>
  <verify>
    Run `npx grep -rn "min-h-\[44px\]" src/components/navigation/GlassHeader.tsx src/pages/AboutPage.tsx` to confirm all 4 elements have the class.
    Run `npm run typecheck` to verify no TypeScript errors.
    Run `npm run lint` to verify no ESLint errors.
  </verify>
  <done>
    All 4 interactive elements (GlassHeader heart, AboutPage share button, AboutPage external links, AboutPage GitHub footer link) have `min-h-[44px]` ensuring 44px minimum touch targets. Visual appearance is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned playXPDing and playErrorSoft exports from celebrationSounds.ts</name>
  <files>src/lib/audio/celebrationSounds.ts</files>
  <action>
    Remove the `playXPDing` function (exported, lines ~149-166) and the `playErrorSoft` function (exported, lines ~176-220) from `src/lib/audio/celebrationSounds.ts`.

    Before removing, verify:
    1. Neither function is imported in any other file (Grep confirmed zero imports)
    2. Neither function is called internally by other functions in the same file (research confirmed `playCelebrationSequence` does NOT reference either)

    Delete the entire function bodies and their export statements. Do NOT remove any other exports — `playCelebrationSequence`, `playNoteWarm`, and other functions are actively used.

    After removal, verify the remaining exports still work by running the build.
  </action>
  <verify>
    Run `npm run typecheck` — should pass with no errors.
    Run `npm run build` — should complete successfully.
    Grep for `playXPDing` and `playErrorSoft` across src/ — should return zero results.
  </verify>
  <done>
    `playXPDing` and `playErrorSoft` are completely removed from the codebase. No remaining references. Build passes clean.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `npm run build` completes successfully
3. Grep for `min-h-[44px]` in GlassHeader.tsx returns at least 1 match
4. Grep for `min-h-[44px]` in AboutPage.tsx returns at least 3 matches (share button, external links, GitHub link)
5. Grep for `playXPDing` across src/ returns 0 matches
6. Grep for `playErrorSoft` across src/ returns 0 matches
</verification>

<success_criteria>
- GlassHeader heart icon, AboutPage Share button, AboutPage external links, and AboutPage GitHub footer link all have min-h-[44px] ensuring 44x44px minimum touch targets
- playXPDing and playErrorSoft orphaned exports removed from celebrationSounds.ts with zero remaining references
- TypeScript typecheck and production build pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/35-touch-target-tech-debt/35-01-SUMMARY.md`
</output>
