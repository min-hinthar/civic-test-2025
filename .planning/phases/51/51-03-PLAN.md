---
phase: 51-unit-test-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: [DEPS-04, DEPS-05]

must_haves:
  truths:
    - "All 3 pnpm.overrides (bn.js, rollup, serialize-javascript) are re-evaluated against current audit data with documented rationale"
    - "All 2 ignored CVEs (CVE-2026-26996, CVE-2025-69873) are re-evaluated with documented rationale"
    - "react-joyride stable 3.0.0 availability is checked and documented"
    - "Overrides/ignores that are no longer needed are removed from package.json"
  artifacts:
    - path: "package.json"
      provides: "Updated pnpm.overrides and auditConfig.ignoreCves with re-evaluated entries"
      contains: "pnpm"
  key_links:
    - from: "package.json pnpm.overrides"
      to: "pnpm audit output"
      via: "audit re-evaluation"
      pattern: "overrides"
---

<objective>
Re-evaluate dependency overrides and ignored CVEs against current audit data, check react-joyride stable 3.0.0 availability, and document findings.

Purpose: DEPS-04 (audit re-evaluation) and DEPS-05 (react-joyride pin). Ensure overrides/ignores are still necessary and remove any that are no longer needed.
Output: Updated package.json (if changes needed), documented rationale for kept/removed entries.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51/51-CONTEXT.md
@.planning/phases/51/51-PRECONTEXT-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-evaluate dependency overrides, ignored CVEs, and react-joyride status</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
Step 1 — Run production audit:
```bash
pnpm audit --prod
```
Record output. Check if any of the 3 overrides (bn.js >=5.2.3, rollup >=4.59.0, serialize-javascript >=7.0.3) are still resolving vulnerabilities.

Step 2 — Run full audit:
```bash
pnpm audit
```
Record total vulnerabilities and which are in devDep chains. Pre-research found: 0 prod vulnerabilities, 9 dev vulnerabilities (all transitive, unfixable).

Step 3 — For each override, evaluate:
- `bn.js >=5.2.3`: Check if the base package now ships >=5.2.3 (run `pnpm ls bn.js` to see resolved versions). If all resolved versions are already >=5.2.3, the override is unnecessary and should be removed.
- `rollup >=4.59.0`: Check `pnpm ls rollup`. If all resolved versions >=4.59.0, remove override.
- `serialize-javascript >=7.0.3`: Check `pnpm ls serialize-javascript`. If all >=7.0.3, remove override.

Step 4 — For each ignored CVE, evaluate:
- `CVE-2026-26996`: Run `pnpm audit 2>&1 | grep -A5 "CVE-2026-26996"` or check if it still appears. If the vulnerability is fixed in current deps, remove from ignoreCves. If still present, document why it's non-exploitable.
- `CVE-2025-69873`: Same evaluation. Pre-research says "known false positive" — verify this is still the case.

Step 5 — Check react-joyride (DEPS-05):
```bash
npm view react-joyride versions --json 2>/dev/null | grep -E "3\.0\." | tail -10
```
Check if stable 3.0.0 (non-prerelease) is published. Pre-research says it's NOT yet available. If still unavailable, document: "react-joyride 3.0.0 stable not yet published as of YYYY-MM-DD. Continuing with 3.0.0-7 prerelease. Risk: MEDIUM (dynamically imported, SSR-gated, non-critical feature)."

If stable 3.0.0 IS available: update package.json dependency from `"3.0.0-7"` to `"3.0.0"`, run `pnpm install`, verify build passes.

Step 6 — Apply changes to package.json:
- Remove any overrides that are no longer needed (packages now ship fixed versions)
- Remove any ignoreCves entries for vulnerabilities that no longer exist
- Pin react-joyride to 3.0.0 if available (otherwise keep 3.0.0-7)
- If NO changes needed (all overrides still required, CVEs still valid, react-joyride still prerelease): document why each is kept but make no changes to package.json

Step 7 — Verify:
```bash
pnpm install && pnpm audit --prod && pnpm build
```
Ensure 0 production vulnerabilities and successful build.

Document all findings in the SUMMARY:
- For each override: KEPT (reason) or REMOVED (no longer needed because X)
- For each ignored CVE: KEPT (reason) or REMOVED (fixed in version X)
- react-joyride: UPGRADED to 3.0.0 or KEPT at 3.0.0-7 (stable not published)
  </action>
  <verify>
    <automated>cd C:/Users/minkk/Documents/GitHub/civic-test-2025 && pnpm audit --prod 2>&1 | tail -5 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm audit --prod` reports 0 vulnerabilities (or only ignored with documented rationale)
    - `pnpm build` exits 0
    - package.json pnpm.overrides section exists (may have fewer entries if some removed)
    - package.json auditConfig.ignoreCves section exists (may have fewer entries if some removed)
    - If overrides removed: `pnpm ls {package}` confirms all resolved versions meet minimum
    - If overrides kept: documented rationale in SUMMARY explaining why each is still needed
    - react-joyride version in package.json is either "3.0.0" (if stable published) or "3.0.0-7" (if not)
  </acceptance_criteria>
  <done>All 3 overrides re-evaluated with documented rationale (kept or removed based on current data). Both ignored CVEs re-evaluated. react-joyride status documented. package.json updated if changes warranted. Production audit clean. Build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm audit --prod` -- 0 production vulnerabilities
2. `pnpm build` -- successful build
3. `pnpm install --frozen-lockfile` or `pnpm install` -- clean install
4. All changes (if any) documented with rationale
</verification>

<success_criteria>
- 3 overrides evaluated with documented keep/remove rationale
- 2 ignored CVEs evaluated with documented keep/remove rationale
- react-joyride 3.0.0 availability checked and documented
- package.json updated if any changes warranted
- Production audit clean, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/51/51-03-SUMMARY.md`
</output>
