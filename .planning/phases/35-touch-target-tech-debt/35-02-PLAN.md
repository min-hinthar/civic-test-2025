---
phase: 35-touch-target-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useRetry.ts
  - src/components/celebrations/Confetti.tsx
  - src/components/celebrations/index.ts
  - src/components/celebrations/CountUpScore.tsx
  - src/components/ui/Skeleton.tsx
  - src/lib/audio/soundEffects.ts
  - src/lib/motion-config.ts
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - VISC-04

must_haves:
  truths:
    - "No orphaned exports remain for useRetry, useConfetti, OdometerNumber, SkeletonCard, SkeletonAvatar, toggleMute, playSwoosh, STAGGER_FAST, or STAGGER_SLOW"
    - "ROADMAP.md plan checkboxes for phases 29-34 all show [x] instead of [ ]"
    - "ROADMAP.md phase-level checkboxes for phases 29-34 all show [x]"
    - "Build and typecheck pass after all removals"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate completion checkboxes for all v3.0 phases 29-34"
      contains: "\\[x\\] Phase 29"
    - path: "src/lib/motion-config.ts"
      provides: "Motion config without orphaned STAGGER_FAST/STAGGER_SLOW"
  key_links: []
---

<objective>
Remove 9 additional orphaned exports discovered during the v3.0 dead code scan and sync all ROADMAP.md checkboxes for completed phases 29-34.

Purpose: These orphaned exports add to cognitive load and dead code surface area. The stale ROADMAP.md checkboxes misrepresent project status. Cleaning both up completes the tech debt closure for v3.0.

Output: 7 modified source files with dead exports removed, 1 updated ROADMAP.md with accurate checkboxes.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-touch-target-tech-debt/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 9 orphaned exports from v3.0 codebase</name>
  <files>
    src/hooks/useRetry.ts,
    src/components/celebrations/Confetti.tsx,
    src/components/celebrations/index.ts,
    src/components/celebrations/CountUpScore.tsx,
    src/components/ui/Skeleton.tsx,
    src/lib/audio/soundEffects.ts,
    src/lib/motion-config.ts
  </files>
  <action>
    Remove the following orphaned exports. For each, the research confirmed zero imports across the entire src/ directory.

    **1. Delete entire file: `src/hooks/useRetry.ts`**
    - Zero imports. ErrorFallback uses inline retry logic instead.

    **2. Remove `useConfetti` from `src/components/celebrations/Confetti.tsx`**
    - Remove the exported `useConfetti` function (find `export function useConfetti` or `export const useConfetti`).
    - The Confetti component itself is used — only the hook is dead.
    - Also remove the `useConfetti` re-export from `src/components/celebrations/index.ts`.

    **3. Remove `OdometerNumber` from `src/components/celebrations/CountUpScore.tsx`**
    - Remove the exported `OdometerNumber` component.
    - Also remove the `OdometerNumber` re-export from `src/components/celebrations/index.ts`.

    **4. Remove `SkeletonCard` and `SkeletonAvatar` from `src/components/ui/Skeleton.tsx`**
    - Remove both exported components (zero imports — consumers built custom skeleton layouts).

    **5. Remove `toggleMute` from `src/lib/audio/soundEffects.ts`**
    - Remove the exported `toggleMute` function. Settings uses `setSoundMuted` instead.

    **6. Remove `playSwoosh` from `src/lib/audio/soundEffects.ts`**
    - Remove the exported `playSwoosh` function. Zero imports.

    **7. Remove `STAGGER_FAST` and `STAGGER_SLOW` from `src/lib/motion-config.ts`**
    - Remove both exported constants. Zero imports.

    IMPORTANT: Before deleting each item, verify it is not referenced by other code in the same file. Only remove truly dead exports. Keep barrel re-exports in index.ts that correspond to still-living source exports.

    After all removals, run `npm run typecheck` to confirm nothing breaks.
  </action>
  <verify>
    Run `npm run typecheck` — should pass.
    Run `npm run build` — should complete.
    Grep for each removed export name across src/ — should return either 0 results or only the barrel re-export (which should also be removed):
    - `useRetry` → 0 results
    - `useConfetti` → 0 results
    - `OdometerNumber` → 0 results
    - `SkeletonCard` → 0 results
    - `SkeletonAvatar` → 0 results
    - `toggleMute` → 0 results
    - `playSwoosh` → 0 results
    - `STAGGER_FAST` → 0 results
    - `STAGGER_SLOW` → 0 results
  </verify>
  <done>
    All 9 orphaned exports removed. The useRetry.ts file is deleted. Build and typecheck pass clean. Zero remaining references to any removed export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync ROADMAP.md checkboxes for completed phases 29-34</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update ROADMAP.md to reflect actual completion status for v3.0 phases 29-34.

    **Phase-level checkboxes (in the bullet list under "v3.0 World-Class UX"):**
    Change from `- [ ]` to `- [x]` for Phase 29 ONLY (the line starting with `- [ ] **Phase 29:`). Phases 30-34 already have `- [x]`. Do NOT check off Phase 35 or 36.

    **Plan-level checkboxes for Phase 29 (lines ~90-96):**
    Change all 7 plan items from `- [ ] 29-01-PLAN.md` to `- [x] 29-01-PLAN.md` (through 29-07).

    **Plan-level checkboxes for Phase 30 (lines ~110-113):**
    Change all 4 plan items from `- [ ] 30-01-PLAN.md` to `- [x] 30-01-PLAN.md` (through 30-04).

    **Plan-level checkboxes for Phase 31 (lines ~127-131):**
    Change all 5 plan items from `- [ ] 31-01-PLAN.md` to `- [x] 31-01-PLAN.md` (through 31-05).

    **Plan-level checkboxes for Phase 32 (lines ~145-152):**
    Change all 8 plan items from `- [ ] 32-01-PLAN.md` to `- [x] 32-01-PLAN.md` (through 32-08).

    **Plan-level checkboxes for Phase 33 (lines ~166-170):**
    Change all 5 plan items from `- [ ] 33-01-PLAN.md` to `- [x] 33-01-PLAN.md` (through 33-05).

    **Plan-level checkboxes for Phase 34 (lines ~184-186):**
    Change all 3 plan items from `- [ ] 34-01-PLAN.md` to `- [x] 34-01-PLAN.md` (through 34-03).

    IMPORTANT: Do NOT change Phase 35 or Phase 36 checkboxes — those are still in progress / not started.
    IMPORTANT: Do NOT modify the Progress table — it already shows correct completion status.
  </action>
  <verify>
    Grep for `- [ ] 29-` through `- [ ] 34-` in ROADMAP.md — should return 0 results.
    Grep for `- [x] 29-` through `- [x] 34-` in ROADMAP.md — should return plan counts matching 7+4+5+8+5+3 = 32 results.
    Grep for `- [ ] **Phase 29:` in ROADMAP.md — should return 0 results.
    Phase 35 and 36 checkboxes remain unchecked.
  </verify>
  <done>
    All 32 plan checkboxes for phases 29-34 updated from `[ ]` to `[x]`. Phase 29 top-level checkbox updated from `[ ]` to `[x]`. Phases 35-36 remain unchecked. ROADMAP.md accurately reflects project status.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes after all dead code removal
2. `npm run build` completes successfully
3. Grep for all 9 removed exports returns 0 results each
4. `src/hooks/useRetry.ts` file no longer exists
5. ROADMAP.md has 32 `[x]` plan checkboxes for phases 29-34
6. ROADMAP.md Phase 35 and 36 plan items remain `[ ]` or `TBD`
</verification>

<success_criteria>
- All 9 orphaned exports removed with zero remaining references
- useRetry.ts file deleted entirely
- ROADMAP.md plan checkboxes for phases 29-34 all show [x]
- Phase 29 top-level checkbox updated from [ ] to [x]
- Build and typecheck pass clean
- Phase 35/36 checkboxes NOT modified
</success_criteria>

<output>
After completion, create `.planning/phases/35-touch-target-tech-debt/35-02-SUMMARY.md`
</output>
