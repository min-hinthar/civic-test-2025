---
phase: 34-content-about-page
plan: 03
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/components/navigation/GlassHeader.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/LandingPage.tsx
autonomous: true
requirements:
  - CONT-05

must_haves:
  truths:
    - "GlassHeader shows a heart icon that links to /about when showAbout prop is true"
    - "LandingPage GlassHeader includes the heart icon (showAbout prop)"
    - "Settings page has a link to the About page in the Help & Guidance section"
    - "LandingPage has a narrative teaser section that links to #/about"
    - "Heart icon in GlassHeader does not conflict with showSignIn or showBack buttons"
  artifacts:
    - path: "src/components/navigation/GlassHeader.tsx"
      provides: "Heart icon navigation to About page via showAbout prop"
      contains: "showAbout"
    - path: "src/pages/SettingsPage.tsx"
      provides: "About page link in Help & Guidance section"
      contains: "About"
    - path: "src/pages/LandingPage.tsx"
      provides: "Narrative teaser linking to About page"
      contains: "/about"
  key_links:
    - from: "src/components/navigation/GlassHeader.tsx"
      to: "src/pages/AboutPage.tsx"
      via: "Link to='/about'"
      pattern: "to=.*about"
    - from: "src/pages/SettingsPage.tsx"
      to: "src/pages/AboutPage.tsx"
      via: "navigate('/about')"
      pattern: "navigate.*about"
    - from: "src/pages/LandingPage.tsx"
      to: "src/pages/AboutPage.tsx"
      via: "Link to='/about'"
      pattern: "to=.*about"
---

<objective>
Add About page navigation entry points: GlassHeader heart icon, Settings link, and LandingPage narrative teaser.

Purpose: The About page must be discoverable from three locations — the app header (heart icon on public pages), the Settings page (Help & Guidance section), and the landing page (narrative teaser hinting at the story).
Output: Modified GlassHeader, SettingsPage, and LandingPage with links to #/about.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-content-about-page/34-CONTEXT.md
@.planning/phases/34-content-about-page/34-RESEARCH.md
@.planning/phases/34-content-about-page/34-01-SUMMARY.md
@src/components/navigation/GlassHeader.tsx — current GlassHeader implementation
@src/pages/SettingsPage.tsx — current Settings page with SettingsRow/SettingsSection components
@src/pages/LandingPage.tsx — current Landing page structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showAbout heart icon to GlassHeader</name>
  <files>
    src/components/navigation/GlassHeader.tsx
    src/pages/LandingPage.tsx
  </files>
  <action>
**GlassHeader.tsx modifications:**

1. Add `Heart` to lucide-react imports (alongside existing `ArrowLeft`)
2. Add `Link` is already imported from react-router-dom
3. Add `showAbout?: boolean` prop to the `GlassHeaderProps` interface with JSDoc: `/** Show heart icon linking to /about */`
4. Add the heart icon BETWEEN the logo and the action buttons area. It should NOT be mutually exclusive with showSignIn or showBack — it's a separate, independent element.

Implementation:
```tsx
{/* About heart icon — independent of other action buttons */}
{showAbout && (
  <Link
    to="/about"
    className="flex items-center justify-center h-9 w-9 rounded-full text-muted-foreground
               hover:bg-primary/10 hover:text-primary transition-colors"
    aria-label="About this app"
  >
    <Heart className="h-4 w-4" />
  </Link>
)}
```

Place the heart icon in the right side of the header, before the showSignIn/showBack buttons. The layout should be:
- Left: Logo ("Civic Test Prep")
- Right: [Heart icon (if showAbout)] [Sign In button (if showSignIn) OR Back button (if showBack)]

Use a flex container with gap for the right-side elements so they space naturally.

IMPORTANT: The existing `justify-between` on the parent flex handles left/right positioning. Wrap the right-side elements (heart + action button) in a flex container with `items-center gap-2`.

**LandingPage.tsx modifications:**

Update the GlassHeader usage on the landing page to include the heart icon:
```tsx
<GlassHeader showSignIn showAbout />
```

This ensures the heart icon appears on the landing page alongside the Sign In button.
  </action>
  <verify>
Run: `npx tsc --noEmit` — no TypeScript errors.
Verify GlassHeader accepts showAbout prop without breaking existing showSignIn/showBack usage.
Verify LandingPage passes showAbout to GlassHeader.
  </verify>
  <done>GlassHeader renders a heart icon linking to /about when showAbout=true, independently of showSignIn/showBack. LandingPage GlassHeader shows both the heart icon and Sign In button.</done>
</task>

<task type="auto">
  <name>Task 2: Add About link in Settings and narrative teaser on LandingPage</name>
  <files>
    src/pages/SettingsPage.tsx
    src/pages/LandingPage.tsx
  </files>
  <action>
**SettingsPage.tsx modifications:**

Add an "About This App" row to the Help & Guidance section. Place it BEFORE the existing "Replay Onboarding Tour" row (so it appears first in that section). Update the "Replay Onboarding Tour" row to no longer be `isLast` — the new About row should be `isLast` instead.

Wait — actually the About row should come first, and Replay Onboarding should still be last. So:
1. Add the About row WITHOUT `isLast`
2. Keep the Replay Onboarding row WITH `isLast`

The About row should be placed BEFORE Replay Onboarding.

```tsx
<SettingsRow
  label="About This App"
  labelMy="ဤအက်ပ်အကြောင်း"
  description="Our story, mission, and dedications"
  descriptionMy="ဇာတ်ကြောင်း၊ ရည်ရွယ်ချက်နှင့် ဂုဏ်ပြုချက်များ"
  showBurmese={showBurmese}
  action={
    <button
      type="button"
      onClick={() => navigate('/about')}
      className="rounded-xl bg-primary-subtle px-4 py-2 text-sm font-bold text-primary hover:bg-primary-subtle transition-colors min-h-[44px]"
    >
      {showBurmese ? 'ကြည့်ပါ' : 'View'}
    </button>
  }
/>
```

Add `Heart` to the lucide-react imports in SettingsPage if needed for the section icon. Actually, the Help & Guidance section already uses RotateCcw — keep that. The SettingsRow itself doesn't need a separate icon.

**LandingPage.tsx — Narrative teaser section:**

Add a brief narrative teaser section between the "Offline + Mobile section" and the "Bottom CTA" section. This teaser hints at the About page story and links to it.

Claude's Discretion on wording, but it should:
- Be a brief, warm sentence or two about the app's story
- Have a "Read Our Story" link to /about
- Render bilingually (English + conditional Burmese with font-myanmar)
- Match the visual style of surrounding sections (FadeIn wrapper, card-style or inline)
- Use Heart icon from lucide-react for visual warmth

Example structure:
```tsx
{/* About Teaser */}
<section className="py-12 sm:py-16">
  <div className="mx-auto max-w-3xl px-4 sm:px-6">
    <FadeIn>
      <div className="flex flex-col items-center gap-4 rounded-2xl border border-border/60 bg-card p-6 text-center shadow-md">
        <Heart className="h-8 w-8 text-primary" />
        <h3 className="text-lg font-bold text-foreground">
          Built with Heart
        </h3>
        {showBurmese && (
          <p className="font-myanmar text-lg text-muted-foreground">
            [Burmese translation]
          </p>
        )}
        <p className="text-sm text-muted-foreground max-w-md">
          This app was born from a deep love for the Burmese community and the people who have dedicated their lives to education across borders.
        </p>
        {showBurmese && (
          <p className="font-myanmar text-sm text-muted-foreground max-w-md">
            [Burmese translation]
          </p>
        )}
        <Link
          to="/about"
          className="inline-flex items-center gap-2 rounded-xl bg-primary/10 px-5 py-2.5 text-sm font-bold text-primary transition-colors hover:bg-primary/20 min-h-[44px]"
        >
          Read Our Story
        </Link>
      </div>
    </FadeIn>
  </div>
</section>
```

Add `Heart` to the existing lucide-react imports in LandingPage.tsx.
Add `Link` import from react-router-dom (already imported as `useNavigate` — but need `Link` too; check if already imported).

Note: LandingPage.tsx already imports `Navigate, useNavigate` from react-router-dom. Add `Link` to those imports.
  </action>
  <verify>
Run: `npx tsc --noEmit` — no TypeScript errors.
Run: `npm run lint` — no lint errors.
Verify Settings has the About row in Help & Guidance section.
Verify LandingPage has the narrative teaser section with link to /about.
  </verify>
  <done>Settings page has an "About This App" link in Help & Guidance. Landing page has a narrative teaser section with "Read Our Story" link to /about. All three navigation entry points (header heart, Settings, landing teaser) are functional.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `npm run build` succeeds
- GlassHeader renders heart icon when showAbout=true, non-conflicting with other buttons
- LandingPage shows heart icon in header AND narrative teaser section
- Settings Help & Guidance section has About This App row
- All Burmese text has font-myanmar class
- No hex colors used
</verification>

<success_criteria>
1. Heart icon appears in GlassHeader on the landing page, linking to #/about
2. Settings page has About link in Help & Guidance section
3. Landing page has a narrative teaser section linking to #/about
4. All three navigation paths correctly reach the About page
</success_criteria>

<output>
After completion, create `.planning/phases/34-content-about-page/34-03-SUMMARY.md`
</output>
