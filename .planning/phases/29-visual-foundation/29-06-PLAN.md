---
phase: 29-visual-foundation
plan: 06
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/hub/AchievementsTab.tsx
  - src/components/hub/StatCard.tsx
  - src/components/interview/ChatBubble.tsx
  - src/components/interview/InterviewResults.tsx
  - src/components/interview/InterviewSession.tsx
  - src/components/interview/InterviewTranscript.tsx
  - src/components/interview/LongPressButton.tsx
  - src/components/navigation/NavBadge.tsx
  - src/components/practice/PracticeConfig.tsx
  - src/components/progress/SkillTreePath.tsx
  - src/components/results/CategoryBreakdown.tsx
  - src/components/social/BadgeHighlights.tsx
  - src/components/sort/MissedCardsList.tsx
  - src/components/sort/SwipeableStack.tsx
  - src/components/ui/BurmeseSpeechButton.tsx
  - src/components/ui/SpeechButton.tsx
  - src/components/interview/TextAnswerInput.tsx
  - src/components/sessions/ResumeSessionCard.tsx
autonomous: true
requirements:
  - VISC-02

must_haves:
  truths:
    - "Zero occurrences of text-[10px] remain in the codebase"
    - "All former text-[10px] usages now use text-caption"
    - "Other arbitrary font sizes (text-[11px], text-[13px]) are migrated to nearest scale step"
    - "No visual regression — text-caption renders at 10px/14px with 0.01em letter-spacing"
  artifacts:
    - path: "src/components/hub/AchievementsTab.tsx"
      provides: "Migrated text-[10px] to text-caption"
    - path: "src/components/navigation/NavBadge.tsx"
      provides: "Migrated text-[10px] to text-caption"
  key_links:
    - from: "tailwind.config.js"
      to: "src/**/*.tsx"
      via: "text-caption utility class from fontSize config"
      pattern: "text-caption"
---

<objective>
Migrate all arbitrary font size values to the semantic typography scale.

Purpose: With the typography scale defined in Plan 01, migrate the 23 `text-[10px]` occurrences across 16 files to `text-caption`, and 2 other arbitrary sizes to their nearest scale steps (VISC-02). This validates the scale and eliminates the most common arbitrary values.
Output: 18 component files updated with zero arbitrary font sizes remaining.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-visual-foundation/29-01-SUMMARY.md
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate text-[10px] to text-caption across all 16 files</name>
  <files>src/components/hub/AchievementsTab.tsx, src/components/hub/StatCard.tsx, src/components/interview/ChatBubble.tsx, src/components/interview/InterviewResults.tsx, src/components/interview/InterviewSession.tsx, src/components/interview/InterviewTranscript.tsx, src/components/interview/LongPressButton.tsx, src/components/navigation/NavBadge.tsx, src/components/practice/PracticeConfig.tsx, src/components/progress/SkillTreePath.tsx, src/components/results/CategoryBreakdown.tsx, src/components/social/BadgeHighlights.tsx, src/components/sort/MissedCardsList.tsx, src/components/sort/SwipeableStack.tsx, src/components/ui/BurmeseSpeechButton.tsx, src/components/ui/SpeechButton.tsx</files>
  <action>
Perform a mechanical find-and-replace across all 16 files:

**Replace:** `text-[10px]`
**With:** `text-caption`

This is a 1:1 replacement — the `text-caption` utility generates the same 10px font-size with the added benefit of a consistent 14px line-height and 0.01em letter-spacing from the Tailwind config.

In each file:
1. Open the file
2. Replace every occurrence of `text-[10px]` with `text-caption`
3. Do NOT change any other classes on the same element
4. Do NOT change any `text-xs` (12px) or `text-sm` (14px) usages — those are already valid Tailwind defaults

Total expected replacements: ~23 across 16 files.

After replacing, verify no `text-[10px]` remains in the codebase with a global grep.
  </action>
  <verify>
Run `npm run build` -- must succeed.
Run `grep -r "text-\[10px\]" src/ --include="*.tsx" --include="*.ts"` -- should return 0 matches.
Run `grep -r "text-caption" src/ --include="*.tsx" --include="*.ts" | wc -l` -- should return ~23 matches.
  </verify>
  <done>All 23 text-[10px] occurrences across 16 files are migrated to text-caption with no remaining arbitrary 10px values.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining arbitrary font sizes to nearest scale step</name>
  <files>src/components/interview/TextAnswerInput.tsx, src/components/sessions/ResumeSessionCard.tsx</files>
  <action>
Two additional arbitrary font sizes exist:

1. **TextAnswerInput.tsx** — has `text-[11px]`. Migrate to `text-body-xs` (12px). The 1px difference is visually negligible and aligns the component with the scale.

2. **ResumeSessionCard.tsx** — has `text-[13px]`. Migrate to `text-body-sm` (14px). The 1px difference is visually negligible and aligns with the scale.

For each file:
- Find the `text-[11px]` or `text-[13px]` class
- Replace with the corresponding semantic class
- Preserve all other classes on the element

After replacing, verify no arbitrary font size values remain:
- `grep -rP "text-\[\d+px\]" src/ --include="*.tsx" --include="*.ts"` should return 0 matches.
  </action>
  <verify>
Run `npm run build` -- must succeed.
Run `grep -rP "text-\[\d+px\]" src/ --include="*.tsx" --include="*.ts"` -- should return 0 matches (zero arbitrary font sizes).
  </verify>
  <done>All arbitrary pixel font sizes (text-[10px], text-[11px], text-[13px]) are migrated to the semantic typography scale. Zero arbitrary font sizes remain in the codebase.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Zero `text-[Npx]` occurrences in src/**/*.tsx or src/**/*.ts
- ~25 new `text-caption` usages across the codebase
- `text-body-xs` and `text-body-sm` used for the former 11px and 13px values
</verification>

<success_criteria>
All arbitrary font size values are eliminated from the codebase. Every screen uses only the semantic typography scale or Tailwind's default size classes. The text-caption utility is the most-used semantic font size with ~23 consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/29-visual-foundation/29-06-SUMMARY.md`
</output>
