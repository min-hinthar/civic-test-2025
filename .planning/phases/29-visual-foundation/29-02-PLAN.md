---
phase: 29-visual-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/results/QuestionReviewList.tsx
  - src/styles/tokens.css
autonomous: true
requirements:
  - VISC-01
  - VISC-03

must_haves:
  truths:
    - "Spacing grid standard is documented with approved exceptions"
    - "QuestionReviewList filter buttons use rounded-xl instead of rounded-md"
    - "Border radius mapping is documented per component type"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Spacing grid documentation and border radius mapping reference"
      contains: "4px grid"
    - path: "src/components/results/QuestionReviewList.tsx"
      provides: "Fixed filter button radius"
      contains: "rounded-xl"
  key_links:
    - from: "src/styles/tokens.css"
      to: "tailwind.config.js"
      via: "radius tokens registered in Tailwind borderRadius config"
      pattern: "radius-xl"
---

<objective>
Document the spacing grid standard and fix the border radius inconsistency.

Purpose: Formalize the 4px spacing grid as the project standard (VISC-01), document the component-to-radius mapping (VISC-03), and fix the one known radius violation in QuestionReviewList.
Output: Updated tokens.css with spacing/radius documentation; fixed QuestionReviewList filter buttons.
</objective>

<execution_context>
@C:/Users/minkk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/tokens.css
@src/components/results/QuestionReviewList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document spacing grid and radius mapping in tokens.css</name>
  <files>src/styles/tokens.css</files>
  <action>
Enhance the existing spacing and border radius documentation sections in `tokens.css`:

**Spacing section** -- update the comment block above the `--space-*` custom properties to include:

```css
/* ── Spacing Grid (4px base, Tailwind enforced) ── */
/* Standard: All padding/margin values use Tailwind's default spacing scale (4px grid).
 * Approved exceptions (do NOT change):
 *   gap-[2px]     — SegmentedProgressBar segment gaps
 *   bottom-[140px] — TestPage toast offset
 *   h-[5px]        — decorative card accent stripes
 * Half-steps (0.5 = 2px) are valid Tailwind values, not exceptions.
 */
```

**Border Radius section** -- update the comment block above the `--radius-*` custom properties to include the component mapping:

```css
/* ── Border Radius (component mapping) ── */
/* rounded-full  = pill buttons, badges, circles, avatar rings */
/* rounded-xl    = standard action buttons, inputs, filter chips */
/* rounded-2xl   = cards, modals, containers, dialogs */
/* rounded-lg    = tooltips, nav chrome elements */
/* rounded-md    = flag toggles, inline secondary actions ONLY */
/* rounded-sm    = data-viz cells ONLY */
/* rounded-none  = full-bleed nav bars */
/*
 * Rule: New components MUST use the mapping above.
 * Exception process: document in this comment block with rationale.
 */
```
  </action>
  <verify>Run `npm run lint:css` -- must pass with no errors.</verify>
  <done>tokens.css documents the 4px spacing grid standard with 3 approved exceptions, and the component-to-radius mapping with enforcement guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Fix QuestionReviewList filter button radius</name>
  <files>src/components/results/QuestionReviewList.tsx</files>
  <action>
In `QuestionReviewList.tsx`, find the three filter buttons ("All", "Incorrect", "Skipped") that use `rounded-md` and change them to `rounded-xl` to match the established button radius standard.

There are 3 instances of `rounded-md` in the filter button className strings. Change each `rounded-md` to `rounded-xl`.

Do NOT change any other styling -- only the border radius class on these specific filter buttons.
  </action>
  <verify>
Run `npm run build` -- must succeed.
Run `grep "rounded-md" src/components/results/QuestionReviewList.tsx` -- should return 0 matches (all instances replaced).
Run `grep "rounded-xl" src/components/results/QuestionReviewList.tsx` -- should return 3 matches.
  </verify>
  <done>All three filter buttons in QuestionReviewList use `rounded-xl` consistent with the button radius standard.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run lint:css` passes
- No `rounded-md` remains in QuestionReviewList.tsx filter buttons
- tokens.css contains spacing grid documentation with 3 approved exceptions
- tokens.css contains component-to-radius mapping
</verification>

<success_criteria>
The 4px spacing grid is formally documented with approved exceptions. The border radius component mapping is documented. The one known radius inconsistency (QuestionReviewList filter buttons) is fixed from rounded-md to rounded-xl.
</success_criteria>

<output>
After completion, create `.planning/phases/29-visual-foundation/29-02-SUMMARY.md`
</output>
